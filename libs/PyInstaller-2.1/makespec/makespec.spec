# -*- mode: python -*-
a = Analysis(['makespec', '..\\..\\ftk_controller.spec'],
             pathex=['D:\\ftk controller\\libs\\PyInstaller-2.1\\makespec'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='makespec.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='makespec')
